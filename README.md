# setl-transport-service-user
Front-end for Nugitech's Setl-transport-service application

# Security
- CSRF Protection is enabled for all requests except ajax request. Excluded URIs are added to the config array.
